import {TransformObservableToItemSelectPipePipe} from './transform-observeble-to-item-select.pipe';

describe('TransformObservebleToItemSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformObservableToItemSelectPipePipe();
    expect(pipe).toBeTruthy();
  });
});
