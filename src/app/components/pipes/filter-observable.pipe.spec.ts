import { FilterObservablePipe } from './filter-observable.pipe';

describe('FilterObservablePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObservablePipe();
    expect(pipe).toBeTruthy();
  });
});
